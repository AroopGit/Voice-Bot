# Vahak India Voice Bot - Dependencies
# Optimized for Hindi, English, and Hinglish

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.6

# Speech-to-Text (Faster-Whisper)
faster-whisper>=0.10.0
numpy>=1.24.0

# Vector Database (Qdrant)
qdrant-client>=1.7.0

# Embeddings (Sentence-Transformers - Multilingual)
sentence-transformers>=2.2.0
torch>=2.0.0

# LLM (Groq API)
groq>=0.4.0

# Text-to-Speech (Edge-TTS - Indian voices)
edge-tts>=6.1.0

# Language Detection (Simple, rule-based - no heavy ML deps needed)
# langdetect and fasttext removed - using custom Hindi/English/Hinglish detector

# Caching
cachetools>=5.3.0

# Logging
loguru>=0.7.0

# HTTP Client
httpx>=0.25.0
aiofiles>=23.0.0

# WebSocket Support (included in uvicorn[standard])
websockets>=11.0

# Audio Processing
soundfile>=0.12.0
